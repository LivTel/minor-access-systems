#!/bin/csh

# -----------------------------------------
# Args list for POS Control Relay (FTN).
# -----------------------------------------

set KEYSTORE     = certs/con.private
set KEYPASS      = geronimo
set TRUSTSTORE   = certs/rtoc.public

set DEBUG = 
#set DEBUG = -Djavax.net.debug=all

set CONFIG = ${POS_CONFIG}/ftn_pos.properties
